# Resolve AI installer internals

Normal users should run the root launchers:

- Windows: `Install Resolve AI.bat`
- macOS: `Install Resolve AI.command`

These scripts contain the platform install logic and are called by the launchers:

- `install.ps1`
- `install.sh`

